return {
	"mg979/vim-visual-multi",
	branch = "master", -- optional: recommend explicitly if you want master branch
	keys = {
		{ "<C-n>" },
		{ "gV" },
	},
	config = function()
		-- nothing to configure by default —
		-- plugin works out of the box
	end,
}
